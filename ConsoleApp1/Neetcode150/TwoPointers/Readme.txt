Generally works when array is sorted or when dealing with subarrays/substrings
a. Constant Window
b. Longest subarray/substring where <condition>
c. Number of sub arrays where <condition>
d. Shortest/ minimum window <condition>

Usually consecutive word is also there
(Sliding Window + Two pointers)